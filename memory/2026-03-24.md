# 2026-03-24

## Member Skill Demo / OpenClaw 适配方式
- 已将下载的 `member-skill-demo` 整理为本地可复用的 OpenClaw 适配方式。
- 统一调用契约：`{"client":"openclaw","response_mode":"compact","license":"...","input":"..."}`。
- 新增稳定适配层：`skills/member-skill-demo/openclaw_adapter.py`
  - 支持 CLI 参数、stdin JSON、纯文本 stdin
  - 强制 UTF-8 输出，避免中文乱码
  - 对 403 单独输出 `membership check failed`
  - 默认输出 backend 的 `answer` 字段，尽量做到“所见即所得”
- 新增 skill 说明：`skills/member-skill-demo/SKILL.md`
- 这条适配方式的核心原则：
  - 轻量封装，不把大 prompt 落本地
  - 入参统一，方便 OpenClaw / UI / 脚本共用
  - 输出尽量保持原样，减少工具痕迹
